[pytest]
# `pytest -q` ที่ root เก็บทั้ง tests/ และ ocr_pipeline/tests/
testpaths = tests ocr_pipeline/tests
# ocr_pipeline/work/ เป็นผลลัพธ์ต่อ run (gitignored) — ไฟล์ที่หลงเหลืออยู่ในนั้นห้ามถูก collect
norecursedirs = ocr_pipeline/work .git .venv venv *.egg-info
