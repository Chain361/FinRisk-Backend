# Backend runtime
fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.*
psycopg[binary]==3.*   # PostgreSQL driver — ใช้ทั้ง API และ seed_database.py
bcrypt==4.*             # password hashing (มี salt ในตัว)
PyJWT==2.*              # ออก/ตรวจ JWT access token
python-multipart==0.0.* # FastAPI Form/File (endpoint อัปโหลด CSV)
google-genai==2.*       # Gemini API — chatbot orchestration (src/services/chatbot.py)
python-dotenv==1.*      # โหลดค่าใน .env (เช่น DATABASE_URL ของ shared dev DB) ตอน local dev
pinecone>=9,<10         # vector search เนื้อหาเอกสารเต็ม (integrated-inference index — ไม่ต้องเรียก embedding API เอง)
                        # SDK 9 มี upsert_records/search ในตัว (ไม่ต้องลง pinecone-plugin-records)
                        # ⚠️ SDK 9 คืน hit เป็น msgspec Struct (.id/.score/.fields) ไม่ใช่ dict คีย์ '_id'/'_score'
                        #    แบบ SDK 7/8 — src/services/retrieval.py::_normalize_hit รองรับทั้งสองแบบไว้แล้ว

# Observability / evaluation — LangSmith (ดู docs/langsmith_eval_plan.md)
# ⚠️ เป็น optional dependency จริงๆ: ไม่ลงก็รันระบบได้ครบ (src/observability.py ทำ decorator
#    ให้เป็น no-op) และ `pytest -q` ผ่านทั้งสองแบบ — อย่าเขียนโค้ดที่ import langsmith ตรงๆ
#    นอก src/observability.py กับ evals/
# ⚠️ LangSmith ไม่ต้องใช้ LangChain — repo นี้จึงไม่มี langchain/langgraph เป็น dependency
#    (ถ้าจะเพิ่มภายหลัง ดูเงื่อนไขในแผน §5 ก่อน — มีข้อห้ามเรื่อง agent executor)
langsmith>=0.3.4      # >=0.3.4 เพราะรองรับ extra [pytest]; wrappers.wrap_gemini ยังเป็น beta

# Dev / test
httpx==0.28.*         # ใช้โดย fastapi.testclient (ปรับจาก 0.27 — google-genai บังคับ >=0.28)
pytest==8.*

# Data extraction / ocr_pipeline
# ตั้งใจซ้ำกับ ocr_pipeline/requirements.txt — ที่นั่นเป็นชุดของ pipeline ที่ติดตั้งแยกได้
# (deploy OCR โดยไม่ต้องลง FastAPI) ถ้าแก้เวอร์ชันต้องแก้ทั้งสองไฟล์ให้ตรงกัน
pdfplumber>=0.11
rapidfuzz>=3.0
